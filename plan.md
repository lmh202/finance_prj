# 改进版风险引擎设计方案（HAR-X / 第四个自建模型）

> 目标：在现有 `scripts/train_risk_engine.py` 的 HAR 基础上，构建一个**自建的第四个模型**，
> 在同一套时间划分与评价指标下**可证明地优于 HAR**，用于项目要求的四模型对比
> （naive / GARCH / EGARCH / **自建**，HAR 作为被超越的基准）。

---

## 一、可行性判断：**可行，但收益幅度有限**

结论先行：**值得做，且大概率能拿到一个真实但温和的提升**；不要期待量级上的飞跃。
判断依据来自本项目已经跑出来的两条硬证据，而非文献直觉。

### 1.1 证据一：更强的非线性模型已经失败过

`train_risk_engine.py:179` 中的 `gbm_feats` 已经把**全部信息**喂给了梯度提升树：

```python
gbm_feats = FEATS_BASE + ["l_garch", "l_egarch", "log_count", "disagreement", "has_news"]
```

即 HAR 的 6 个基础特征 **＋** GARCH 预测 **＋** EGARCH 预测 **＋** 新闻量 **＋** 分歧度。
结果：测试集上 **HAR 仍然获胜**（DM 检验 t > 0）。

这说明瓶颈**不在特征供给，也不在模型容量**。该时间尺度上的波动率由一个
缓慢、持续、近似对数线性的成分主导，树模型并不比对数线性拟合更能利用它。

**推论：再上一个更灵活的黑箱（LSTM / TFT / 更深的树）极可能重复同样的失败**，
且会重蹈 EGARCH 在 SLV 等薄数据标的上数值崩溃（预测值 2.77e-82）的覆辙。

### 1.2 证据二：文献中 HAR 本就极难被超越

HAR 是波动率预测里出了名的"难打的基准"。被广泛引用的改进
（如 HARQ，Bollerslev / Patton / Quaedvlieg 2016）在 QLIKE / R² 上的增益也只是
**个位数百分比**，而非数量级差距。

### 1.3 已排除的方向（不再重复试错）

| 方向 | 排除理由 |
|---|---|
| 更深的树 / 更多轮次 | 1.1 已证伪，属于重复已定论的实验 |
| LSTM / TFT / Transformer | 同上，且样本量与信噪比不支持 |
| 引入新数据源（分钟级、期权隐含波动率） | 当前数据集不具备，超出项目范围 |

---

## 二、模型设计：HAR-X

在现有池化 HAR 之上叠加四个组件，**全部保持线性**（因此过拟合风险不高于 HAR 本身）。

### (a) 杠杆 / 非对称项 —— 新增

**动机**：同等幅度下，**下跌日**比上涨日更能抬升未来波动（杠杆效应）。
这正是 EGARCH 试图刻画的东西 —— EGARCH 的**形态假设是合理的**，
崩掉的是它在薄数据上的数值稳定性，而不是这个经济直觉。

**做法**：对当日冲击项增加一条负收益专属斜率。

```python
d["l_absret_neg"] = d["l_absret"] * (ret < 0)      # 仅下跌日生效
```

**成本**：零新增数据，仅一个新特征。

### (b) 精度加权（HARQ 思路的降级改编）—— 新增

**动机**：HARQ 的核心思想是——当 `RV_t` 本身**估计噪声大**时，应当调低它的系数。
原版用**已实现四次幂变差（realized quarticity）**度量噪声，需要**日内数据**。

> ⚠️ **必须诚实说明**：本项目只有日频 OHLC，**无法复现真正的 HARQ**。
> 下面是同一逻辑的粗糙代理，**不得在报告中标称为"HARQ"**，
> 应表述为"受 HARQ 启发的精度加权"。

**代理做法**：用窗口内平方收益的离散度作为噪声代理 —— 一个跳跃剧烈的窗口，
其 RV 估计的可信度低于平滑窗口。

```python
q5 = (ret ** 2).rolling(5).std()                   # 窗口内平方收益的离散度
d["l_rv5_x_q"] = d["l_rv5"] * zscore(np.log(q5))   # 交互项：噪声大 → 削弱该项
```

**风险提示**：这是整套设计中**最不确定能否兑现的一环**。若在验证集上无增益，
**第一个删掉它**，并在报告中如实记录"尝试过、无效"。

### (c) 新闻项 —— ⚠️ 已存在，本方案中作为**组合成分**而非新贡献

**更正**：`train_risk_engine.py:58` 已经定义了

```python
FEATS_NEWS = FEATS_BASE + ["log_count", "disagreement", "has_news"]
```

并在 `train_risk_engine.py:243-258` 对 12 只新闻覆盖股做了完整的
"HAR vs HAR+news" 线性增量检验（含 DM 检验）。**这一步已经做过了。**

因此本方案中，新闻项的定位是：**与 (a)(b) 组合后再评估其边际贡献**，
而不是一个新想法。（此前口头方案把它列为"新增"，属于表述错误，此处更正。）

**已跑出的结果（2026-07-20 复跑）**：

| 期限 | HAR QLIKE | HAR+news QLIKE | Δ | DM t | p | 结论 |
|---|---|---|---|---|---|---|
| **h=5** | 0.4459 | **0.4399** | −0.0061 | **−3.86** | **0.000112** | **新闻显著有效** |
| h=20 | 0.1709 | 0.1709 | −0.0000 | −0.11 | 0.91 | 无增益 |

（12 只新闻覆盖股，n=8,217 / 8,052；两侧同为线性模型，模型类别固定）

**这是本项目目前最干净的新闻正结果**：相对 QLIKE 改善 1.4%，p=1e-4。
机制上也自洽 —— 新闻强度是**短期现象**，今天的头条爆发能说明下周，说不了下个月。

> ⚠️ **必须在报告中说明的选择性偏差**：`log_count` 与 `disagreement` 这两个特征，
> 是依据 `validate_news_metrics.py` 挑选的，而该脚本的相关性是在
> **2013–2023 全样本（含测试期）** 上计算的。DM 检验本身是干净的（模型仅用训练集拟合），
> 但**特征选择环节看过测试数据**。严谨做法：把该筛选过程限制在 train+val 上重跑，
> 确认仍然选出同样两个特征。**"我认为会"不算证据，必须跑。**

**对 v2 设计的影响**：新闻项的价值集中在 **h=5**，v2 应以 h=5 为主战场。

保留理由：`log_count` 与 `disagreement` 已通过 GARCH(1,1) 控制检验
（偏相关 ρ ≈ 0.015–0.03，虽被腰斩但显著为正），信号是真实的；
GBM 拿到它们却输给 HAR，说明**树不是承载这种微弱线性信号的合适载体**，
而不是信号不存在。

### (d) 区间校准：Student-t 尾部修正 —— 新增，**预计是最大单项收益**

**动机**：现有诊断显示这是**分布问题，不是水平问题**。
但需**修正此前的表述** —— 查 `risk_engine_results.csv` 后可见，覆盖率缺口
**主要是 h=5 的问题，h=20 已基本达标**：

| 标的 / 期限 | 覆盖率 | 目标 |
|---|---|---|
| 池化 TEST **h=5** | **0.9246** | 0.95 |
| 池化 TEST h=20 | 0.9459 | 0.95 ✅ 已接近 |
| NVDA h=5（`scripts/har_example.py`） | 0.9010 | 0.95 |

因此 (d) 的收益范围比初稿宣称的窄：**它是一个短期修正，不是全局修正**。
这与 (c) 的结论方向一致 —— **v2 的主战场是 h=5**。

点预测本身是准的（R²(log) = 0.46 @5d / 0.68 @20d），
但用高斯 ±1.96σ 画的区间**太窄**，肥尾击穿。

**做法**：在**验证集**上拟合标准化残差的自由度 ν，用 t 分位数替换 1.96。

```python
z = fret_val / (sigma_hat_val * np.sqrt(h))        # 标准化实际收益
nu = stats.t.fit(z, floc=0)[0]                     # 仅在 VAL 上拟合
band = stats.t.ppf(0.975, nu) * sigma_hat * np.sqrt(h)
```

**特点**：**不需要任何新预测变量** —— 它是加在同一个点预测之上的分布修正，
是四项中最便宜、最诚实、也最直接服务于下游仓位管理的一项
（区间宽度直接进 `recommend_daily()` 的 sizing 分母）。

---

## 三、训练与评估协议（与现有基准**完全一致**，否则对比无效）

### 3.1 数据划分（沿用 `train_risk_engine.py:51-55`）

```
训练  2013-01 ~ 2018-12
验证  2019-01 ~ 2020-12
测试  2021-01 ~ 2023-12
```

- **按时间切分，绝不按股票切分** —— 市场 regime 同时冲击所有标的，按股票切会泄漏。
- 每个接缝处保留 **EMBARGO = 20 天**（代码中实际用 `EMBARGO * 1.5` 天），
  因为 h 天前瞻标签会跨越边界。
- 预测期 `HORIZONS = [5, 20]`。

### 3.2 评价指标（沿用 `evaluate()`）

| 指标 | 含义 | 目标 |
|---|---|---|
| **QLIKE** | 主指标，对代理噪声稳健，惩罚低估风险 | 越低越好 |
| RMSE_log / R²_log | 对数波动率上的拟合优度 | R² 越高越好 |
| MZ a, b | Mincer-Zarnowitz 校准（偏差与尺度） | a=0, b=1 |
| **coverage** | P(\|h 日收益\| ≤ 区间) | ≈ 0.95 |
| **DM 检验** | Diebold-Mariano（Newey-West 修正） | t < 0 且 p < 0.05 |

### 3.3 纪律性约束（防 p-hacking）

1. **所有超参数、组件取舍，只允许看验证集（VAL）决定。**
2. **测试集（TEST）只跑一次**，跑完即定稿，不允许回头调参再跑。
3. 组件 (a)(b)(c) 的取舍在 VAL 上做**消融**，**无论结果好坏全部写进报告**
   （包括 (b) 失败的情况）。
4. 对数→水平的 smearing 修正沿用现有规则：`exp(var(resid)/2)`，
   **在验证集上估计**（`train_risk_engine.py:198-201`），对所有模型一视同仁。

---

## 四、验收标准（**动手前先锁定**）

> 以下规则在实施前确定，实施后不得修改。

**判定「HAR-X 优于 HAR」当且仅当：**

- **主标准**：测试集上 HAR-X 的 QLIKE 低于 HAR，且 DM 检验
  **在 h=5 或 h=20 至少一个期限上 t < 0 且 p < 0.05**，
  同时**在另一个期限上不出现显著劣化**（不得 t > 0 且 p < 0.05）。
- **次标准（区间质量）**：覆盖率比 HAR 更接近 0.95。
  次标准**可单独成立并单独报告** —— 即使点预测未显著改善，
  区间校准的改善本身就是对下游仓位管理有价值的成果。

**若主、次标准均未达成**：如实记录为**否定结果**，
在报告中论证「HAR 在该数据规模与频率下已接近可达上限」，
并保留 HAR 作为生产模型。这**同样是一个合格的结论**
（与本项目此前对 FinBERT 方向性信号的处理方式一致）。

---

## 五、实施步骤

| 步骤 | 内容 | 产出 |
|---|---|---|
| 1 | 新建 `scripts/train_risk_engine_v2.py`，复用 `build_stock()` / `log_feats()` / `evaluate()` / `dm_test()` | 不修改原文件，保证基准可复现 |
| 2 | 在 `build_stock()` 之上扩展特征 (a)(b) | 新增列 `l_absret_neg`, `l_rv5_x_q` |
| 3 | 在 VAL 上做组件消融：base / +a / +a+b / +a+b+c | 消融表 |
| 4 | 在 VAL 上拟合 Student-t 自由度 ν | 区间校准参数 |
| 5 | **测试集单次评估**，输出与现有格式一致的对比表 | `reports/risk_engine/risk_engine_v2_results.csv` |
| 6 | 可视化：沿用 `har_example.py` 的双面板图，叠加新旧区间对比 | `reports/risk_engine/fig_harx_vs_har.png` |

**依赖**：`numpy` / `pandas` / `scipy` / `scikit-learn` / `arch` —— **全部已安装，无新增依赖**。

---

## 六、预期结果（诚实版）

- **点预测精度（QLIKE / R²_log）**：相对 HAR 提升**幅度较小，约个位数百分比**，
  与文献水平及本项目 GBM 的结果一致。显著性能否达标存在真实的不确定性。
- **区间覆盖率**：这是**最可能兑现**的收获，预计可从 ~92.5% 推进到接近 95%。
- **可报告的核心价值**：HAR-X 是一个**真正的自建扩展**，
  其每一个组件都可以追溯到**本项目自己验证过的实证发现**
  （(a) ← EGARCH 的非对称假设；(c) ← 通过 GARCH 控制检验的新闻强度信号；
  (d) ← `har_example.py` 暴露出的肥尾覆盖不足），
  而不是从外部空降一个未经检验的复杂模型。这一点在答辩中比多零点几个百分点更有说服力。
